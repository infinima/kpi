{
  "name": "kpi",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["frontend"],
  "description": "Full-stack: Node.js server + Vite React TS frontend",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm --prefix frontend install",

    "dev": "concurrently -k -n SERVER,WEB \"npm run dev:server\" \"npm run dev:front\"",
    "dev:server": "nodemon server.js",
    "dev:front": "npm --prefix frontend run dev",

    "build": "npm --prefix frontend run build",
    "start": "node server.js",
    "serve": "npm run build && npm run start",

    "preview:front": "npm --prefix frontend run preview",

    "typecheck:front": "npm --prefix frontend run typecheck",

    "bump:patch": "npm version patch -ws -m \"chore: bump to %s\"",
    "bump:minor": "npm version minor -ws -m \"chore: bump to %s\"\n",
    "bump:major": "npm version major -ws -m \"chore: bump to %s\"\n"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.0"
  }
}